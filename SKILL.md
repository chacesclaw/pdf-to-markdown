---
name: student-paper-columns
description: 把学生提交的论文/项目报告/竞赛作品 PDF（多栏、表格、中英文皆有，版式往往比标准学术期刊更杂）抽取成可读正文。触发词「学生论文」「多栏 PDF」「论文解析」「答卷/作品 PDF 提取」，或用户甩来一批学生提交的 PDF 要求整理/校对/判断格式还原效果。fork 自 lecture-notes skill 的课前阅读材料管线（同一套 pdf-extract-columns.py + text-normalize.py 起点），但学生作品的版式比期刊论文杂得多——英文文档、PPT 导出的封面/表单页、装饰字体标题、信息图/海报页、不缩进靠空行或短行收尾分段的写作习惯都很常见，所以单独维护一份带针对性修复的脚本，不跟讲义场景混用。260922-24 用 Bloom NEC/HOSA 学生作品（12 份高中生课程项目报告 + 9 份 NEC 竞赛论文真实答卷）实测并分批验证过全部修复，含一次「代码改了但没生效」的真实踩坑（忘记 rstrip 导致判据恒不触发）。260924 补了图表/插图抠图+OCR+视觉描述的逐页 Markdown 产出格式（§6），能把图表里的文字和图表本身的走势/趋势描述都写进最终交付物，不再是图片抠出来就没了下文。
version: 1.11.1
---

# 学生论文/多栏 PDF 抽取

处理学生提交的论文、项目报告、竞赛作品 PDF——这类材料的版式比标准学术期刊杂得多：可能是纯英文，
可能是 PowerPoint 导出的封面 + 表单字段 + 叙事正文混合，也可能是信息图/海报式排版。

## 和 lecture-notes skill 的关系

同一个起点（`pdf-extract-columns.py` 的「每块一行 + 首行缩进定段 + 动态分栏」思路），但**不共用脚本**：
- `lecture-notes` 的版本是照着**中文学术期刊双栏论文**调的（张建君等材料），假定分栏候选行必须是中文。
- 这个 skill 的版本额外处理**英文文档、表单/封面页、装饰字体乱码、信息图页**——这些是学生作品的高频版式，
  期刊论文场景不需要，硬塞进 lecture-notes 会增加不相关的复杂度，所以分开维护，各自对自己的场景负责。

两边遇到真正通用、双方都受益的底层修复（比如更精确的页眉页脚识别）可以互相参考，但脚本本身独立演进。

## 0. 先探版式，再决定策略

跟 lecture-notes 一样，动手前先跑 `scripts/probe.py <文件...>` 摸底：

```bash
./venv/bin/python scripts/probe.py *.pdf
```

看三件事：
1. **有没有文字层**（`get_text()` 是否为空）——没有就只能 OCR，见 lecture-notes §5 的思路。
2. **块行数众数是不是 1**——是的话说明是「每行一个 block」的期刊式/表单式版式，用这个 skill 的抽取脚本；
   块行数 > 1（PyMuPDF 自己就能分出完整段落）就不需要这套管线，直接读 `get_text()` 即可。
3. **文档是不是加密/DRM 封装**——见 §3，这个坑比版式问题更常见，务必先查。

## 1. 抽取管线

```bash
ssh chacesm2max 'cd ~/某工作目录 && python3 -m venv venv && ./venv/bin/pip -q install pymupdf'
./venv/bin/python scripts/pdf-extract-columns.py 文件.pdf raw.json
./venv/bin/python scripts/text-normalize.py raw.json art.json
```

跟 lecture-notes 的 `reading-materials.md` §1/§2 是同一套基础方法论（分栏容差用栏宽中位数、段落边界看首行缩进、
跨页跨栏不提前 flush、页眉页脚按多页重复识别），这部分直接抄它的规则，不重复写。以下只写**这个 skill 独有、
针对学生作品新加的四处修复**（260922 用 3 份 HOSA 学生项目报告实测验证过，经过三轮迭代才收敛）：

### 1.1 分栏候选判据从「中文字符」放宽成「任意字母」

原判据 `sum("一"<=c<="鿿" for c in line) > 6` 对纯英文文档恒为 0——`cand` 列表永远是空的，
整个分栏检测机制直接失效，退化成「一个覆盖全页的伪单栏」，英文文档的封面/表单页因此被硬拼成一大段。
改成 `sum(c.isalpha() for c in line) > 6`（中英文字母都算）后，分栏检测才对英文文档真正生效。
**这是根因修复，下面三处修复都建立在这个基础上，不改这条，后面的都无从谈起。**

### 1.2 居中短行（表单字段/封面署名）单独成块，用专门的 `field` 类型标记

**判据要两个条件同时满足，缺一都会出问题**（踩过两次坑才收敛）：

```python
def is_centered(bbox, W, colw, width_ratio=0.65, margin_tol_ratio=0.1, min_margin_ratio=0.05):
    x0, _, x1, _ = bbox
    if (x1 - x0) >= colw * width_ratio: return False   # 必须明显短于正文栏宽
    left, right = x0, W - x1
    if left < W * min_margin_ratio: return False        # 不贴着正文左边距
    return abs(left - right) < W * margin_tol_ratio      # 左右留白对称
```

- **只看「左右边距对称」会大量误伤两端对齐的正文段落**——两端对齐（justify）本身就会让每行左右留白趋于对称，
  第一版只用这一条判据时，把整段两端对齐的英文正文拆成了一句一块，比不修都差。必须先用「行宽 << 栏宽」
  排除掉贴满栏宽的两端对齐行，只留下真正短的、孤立的行。
- **只看「行宽占页面宽度比例」会漏掉长字段行**——比如队员名单很长的 `Team Member Names: ...` 这一行，
  虽然是居中排版，但行宽本身可能超过页面宽度的一半，用页面宽度做分母会误判成「不够短」而漏检。
  改成拿**正文栏宽（colw）**当分母，而不是页面宽度，因为居中字段行的宽度上限应该跟「正常一行文字能占多宽」
  比，不是跟整个页面比。
- **额外加一条内容模式兜底**：`^[A-Z][A-Za-z][A-Za-z /]{1,28}:\s`（形如 `Label: value`），
  不完全依赖几何位置。极少数居中字段行因为换行导致宽度判据失手时，靠这条内容特征还能兜住。
  代价是极少数以「Word: 」开头但其实是正文续写的句子（如 `Conclusion: This online survey was...`）
  会被误判成独立字段，这个副作用比「字段全部拼车」的原问题轻得多，可以接受。
- **必须给这类行单独的块类型（`field`），不能标成 `p`**：`text-normalize.py` 里有一条「未完句合并」规则——
  如果上一段不以句末标点收尾就默认是被版式打断的句子，自动拼接下一段。字段行天生不以句号结尾
  （`Event Name: Health Education` 后面没有句号），如果标成 `p`，会被这条规则原样再拼回一大段，
  等于白修。**改用 `field` 类型，并且在 `text-normalize.py` 的 `finalize()` 里把 `field` 加进
  「原样保留、跳过未完句合并」的类型白名单**（跟 `h`/`title`/`lead`/`q` 同等对待）。

### 1.3 行内嵌入图片留下的占位符字符，抽取阶段直接清掉

PDF 里正文引用行内小图标/图片时，PyMuPDF 抽出来的文字流里会夹带对象替换符 `U+FFFC`
或私有区图标字符（`U+E000`–`U+F8FF`），肉眼看是空白或问号方框，正文读起来会有「不可见的字符污染」。
在 `clean()` 里加一步正则清理：

```python
PUA_RE = re.compile("[" + chr(0xFFFC) + chr(0xE000) + "-" + chr(0xF8FF) + "]")
t = PUA_RE.sub('', t)
```

`text-normalize.py` 里也重复清一遍（防止有人绕过抽取脚本直接喂旧 JSON 进来漏清）。

### 1.4 装饰字体标题乱码：先检测嫌疑，再用 OCR 重新识别替换

海报/封面常用非常规展示字体，这类字体的 cmap 表可能跟标准 Unicode 映射对不上，
PyMuPDF 按字节取字符会取出**错误但看起来像文字**的乱码——不是丢字，是**错字**，脚本层面的字符串清洗
救不回来（原始比特就是错的）。真实案例：`Pulling an All-Nighter: Why Your Brain Needs More Than a 3 A.M. Nap`
被解成 `PullingXanXyllQNighter:XWhyXYourXBrainNeedsXMoreXThanXaX3Xy@M@XNap`——空格被取成了字母 `X`。

判据：**长度 ≥ 15 且完全没有空格的英文串**，正常英文标题不可能连续十几个字符不出现一个空格：

```python
def looks_glyph_broken(t):
    core = re.sub(r'[^A-Za-z]', '', t)
    return len(t) >= 15 and " " not in t and len(core) >= 10
```

命中后，把该标题块在页面上的原始 bbox 渲染成图片（留一点 padding、放大 4 倍保证清晰度），
调 Apple Vision OCR（chacesm2max 的 `~/bin/vocr.swift`）重新识别，用 OCR 结果替换掉乱码文本：

```python
pix = pg.get_pixmap(clip=bbox_with_padding, matrix=fitz.Matrix(4, 4))
# swift ~/bin/vocr.swift <图片路径>
```

**实测效果**：乱码标题从完全不可读，恢复到「基本可读、个别字母 OCR 误识别」（`All-Nighter`→`Al-Nighter`，
`Brain Needs`→`BramnNeeds`）——是真实、可验证的改进，但不是 100% 精确，OCR 本身有误差率，
产出前仍建议执行 skill 的 AI 自己再核对一遍标题（毕竟标题只有一行，核对成本很低），不是留给
用户去做人工校对。

OCR 工具不可用时（非 Mac 环境、找不到 `vocr.swift`）静默跳过，保留原文本，不阻断整体流程——
宁可留着已知的乱码、在文档里如实标注出来，也不能让整个抽取任务因为一个工具缺失而中断。

### 1.5 段落边界不止「首行缩进」一种——空行分段、短行收尾分段也要认

260923 用 9 份 NEC 学生论文（真实答卷，非讲义材料）实测才发现：**原脚本的段落边界判据只认「首行缩进」，
但国际学生写作常见另外两种完全不缩进的分段方式**，不认出来的后果是一整篇论文（甚至跨好几页）被拍扁成
一个几千到一万多字的大块，是这批修复里影响面最大的一类问题：

- **空行分段**：段与段之间空一整行，段内不缩进（最常见）。判据是**行距的相对跳变**，不能用固定字号倍数——
  实测同一批论文里，紧凑排版的段内行距只有 0.7pt，段间空行间距 16.3pt，两者相差 20+ 倍；换一份行距更松的论文，
  这两个绝对值会整体漂移。**做法是先跑一遍摸底**，收集全文「正文行到正文行」的纵向间距取中位数当基准，
  再乘 3 倍当「空行分段」的判定阈值，阈值随每份文档自适应，不是写死的常数：
  ```python
  gaps = [...]  # 摸底阶段收集的正文行间距
  normal_gap = gaps[len(gaps)//2]          # 中位数 = 这份文档的正常单倍行距
  PARA_GAP = max(normal_gap * 3, BODY * 0.3)
  ```
- **短行收尾分段**：段与段之间**不空行、不缩进**，纯粹靠「上一行没顶到栏右边界 + 以句末标点收尾」来判断
  换段（比如一段的最后一行提前结束，下一行紧接着开始新段落，行距跟段内完全一样）。判据要同时满足两个条件，
  缺一都会误判——只看「提前收尾」会把所有自然换行到短单词的行都当成分段，只看「句末标点」会漏掉没有标点
  收尾的情况（如以右括号、右引号收尾）：
  ```python
  short_ended = (prev_line.rstrip()[-1] in '.!?"”\'’」』）'
                 and (栏右边界 - prev_line_x1) > BODY * 3)
  ```
  **踩过一次坑**：第一版忘了 `.rstrip()`，PyMuPDF 抽出来的行文本末尾常带一个空格字符，导致
  `prev_line[-1]` 永远是空格、这条规则形同虚设，跑了但没生效——**代码"看起来在跑"不等于"真的生效"，
  改完必须真的对比抽取结果里的分段有没有变化，不能只看程序有没有报错。**
- **三种判据的优先级**：短行收尾 > 空行跳变 > 首行缩进，三选一命中就 flush，不互斥判断（一份文档可能
  混用多种排版习惯，尤其引言/正文/参考文献分属不同小节时）。

**实测效果**（9 份 NEC 论文批量验证）：多份文档的最大分块从 5,700～15,300 字符降到 2,000～4,600 字符区间，
其中一份从 10,877 字符的单一大块拆成 15 段合理长度的段落，跟原文可见的段落边界基本对应。

**已知副作用**：`field` 类型的正则判据（§1.2）偶尔会把论文正文里的小节引导词（如 `Abstract: ...`、
`Introduction: ...`、`Impacts:`）也当成表单字段单独切出来，导致这段文字跟紧跟着的正文断成两块
（内容不丢、顺序不乱，只是块类型不统一，一个标成 `field`、后续内容标成 `p`）。目前判定是「利大于弊」
（不拦会导致更严重的整段粘连），但如果观察到实际误伤明显，缩小 `FIELD_LABEL_RE` 的匹配范围
（比如要求前后是独立行、且后面不再有本段落续写的迹象）是下一步可以改的方向。

### 1.6 图片插进标题/段落中间，打断了原本正常工作的合并逻辑

260924 补图表抠图功能之后才暴露的新坑：图片是按 `(页码, y0)` 位置插入到文字块序列里的，如果
一张图片恰好夹在「标题第一行」和「标题第二行」之间（比如标题背景图卡在两行标题正中间），或者
夹在一句被版式截断的话中间，`text-normalize.py` 里两处依赖"上一块是什么"的合并逻辑就会失效：
`merge_heads()` 按字号把连续标题行合并回一块，`finalize()` 里的"未完句合并"规则靠"上一段没有
收尾标点=被截断"来判断要不要接着拼——图片一插进来，"上一块"就变成了图片块，两条规则都找不到
真正该接续的文字块，标题被拆成两截、句子被图片生生劈开。

**改法**：两处合并逻辑都加了"图片透明跳过"处理——遇到 `image` 类型的块，先原样放进输出、但不
更新"上一个可合并对象"的指针，下一个文字块依然能正确找到图片之前的那个标题/段落继续合并，图片
本身的位置也不受影响。**这个坑的教训**：任何"看上一个块是什么类型再决定要不要合并"的逻辑，一旦
后来引入了新的块类型（这里是 `image`），都要回头检查这条逻辑会不会被新类型打断——加新功能时不能
只测新功能本身工作正常，还要测它有没有让"看似无关"的旧逻辑失效。

### 1.7 `home=None` 静默丢弃整行内容

`page_columns()` 给每一行找归属栏时，如果这一行的 x0 比所有检测到的栏起点都更靠左（常见于比
正文更外凸的编号列表项/小标题，比如"5.Activity 1: Role Play"缩进比正文栏浅），原逻辑找不到
匹配的栏就直接不分配——这一整行会被无声丢弃，不报错、不留痕迹。开发阶段 AI 逐页对照原文核对
HOSA-1 的抽取结果时真的因为这个丢过内容：一个编号标题连同紧跟着的一句正文完整消失，只能靠 AI
自己逐页通读原文才发现，不是靠人工去查，自动化的
块数量统计完全看不出异常（少了一行，块总数也就少一个，没有任何报错信号）。**改法**：找不到匹配
栏时不再放弃，退回"距离最近的已知栏"（`min(starts, key=lambda x: abs(x - l["b"][0]))`），保证
每一行都有归属，不允许任何一行凭空消失。

### 1.8 页面边距过滤过紧，切掉了页面边缘的正文句子

`run()` 的 `top_frac`/`bot_frac` 参数用来过滤页面物理边缘的出血/裁切线一类内容，原始值是
0.075/0.93。开发阶段 AI 逐页对照原文核对 HOSA-1 时抓到三处整句丢失，查下来全部是正文最后一行恰好落在这条边距线
之外被直接过滤掉（真实坐标 y0=757/752/738，页高 793.5，旧 `bot_frac=0.93` 对应 y=738，卡得
太死，比很多论文实际的正文下边界还靠上）。**改法**：收窄到 0.02/0.98，几乎不再过滤掉任何正文
内容；真正的页眉页脚不依赖这个百分比边距去挡，而是靠 `dead`（多页同一位置反复出现的文本）单独
识别——两套机制分工不同，不能让"挡物理边缘"的粗粒度边距参数顺带把正文也卡没了。

## 2. 已知未解决：信息图/海报式版式

**以上四处修复解决不了「大量插画 + 分栏问答气泡 + 无规则文本框」的信息图页**，260922 用 HOSA 学生作品的
一份 14 页信息图海报实测：即便加了上述全部修复，正文仍然会被拍扁成几个大段落（一段能有 4000+ 字），
标题仍可能被裁切丢字（比如 `A Teen's Guide to Skin and Self-Worth —— Glow up, inside out` 只剩下
`and Self-Worth` + `Glow up, insideout` 两截）。

**根本原因**：这类页面的文本框不是沿着「左右两栏」这种规则几何排布的，而是像杂志排版一样自由摆放、
大小不一、经常紧贴着大幅插画环绕。§1 的所有判据（栏宽中位数、居中检测、字段正则）本质上都假设
「文本沿着少数几条规则的列/行排列」，信息图页不满足这个前提，属于完全不同的版式类别，不是同一套
启发式规则能通过加更多条件覆盖的。

**正确做法不是继续打补丁，是先判断值不值得跑管线**：
- `probe.py` 摸底时，如果一页的图片覆盖面积占比很高（可以用 `pg.get_images()` 数量和 `pg.get_image_bbox()`
  面积估算），或者块行数众数是 1 但 `cand`（分栏候选）比例很低，大概率是信息图页，**直接放弃文本抽取管线，
  改成整页截图保留原版式**（跟 lecture-notes §4 表格处理里「大矩阵直接截图」是同一个思路：重构收益低、
  风险高的时候，截图比强行解析更诚实）。
- 已经跑出来的乱码/拍扁结果，不要直接拿去用，AI 先自己看一眼原图核对再决定是留文本还是换截图。

## 3. 加密/DRM 封装：比版式问题更常见的坑，务必先查

有些学生作品（尤其考试/竞赛平台批量导出的答卷）在服务器上存的就不是标准 PDF 字节，而是被
企业级防泄漏系统（DRM）封装过的格式。260922 实测过一次：文件扩展名是 `.pdf`，下载下来的字节
跟服务器端 MD5 完全对得上（不是下载或传输出错），但文件头是 `%TSD-Header-###%`，PyMuPDF
直接报 `Failed to open file`，根本打不开。

**判断方法**：下载完先用 `file 文件.pdf` 或 `head -c 20 文件.pdf` 看文件头是不是 `%PDF-`，
不是的话说明这份文件在源头就被加密封装了，不是网络传输或哪一步操作出的错，去查传输环节没有用。

**没有轻量的绕过办法**——这类封装通常要专门的解密 SDK/阅读器才能打开，不是重跑脚本或换个下载方式能解决的。
遇到直接如实报告「这份文件打不开、原因是 XX」，问用户要不要找发件人拿未加密原件，不要在这上面继续试错。

## 4. 工具清单

- `scripts/probe.py`：版式探测（有没有文字层、块行数众数、栏起点分布）。
- `scripts/pdf-extract-columns.py`：抽取（含本文档 §1 的四处修复，§6.10 的三处双栏 bug 修复）。
  `compute_body_and_dead()` / `page_columns()` 是模块级函数（260925 从 `run()` 内部的嵌套闭包
  提出来的），外部脚本可以直接复用，不用重新抄一份逻辑。
- `scripts/verify-columns.py`：双栏（及以上）论文核对工具，直接复用上面两个模块级函数，按
  「栏 1 全文、栏 2 全文……」的真实阅读顺序打印每一页的分栏结果。用法：
  `./venv/bin/python scripts/verify-columns.py <PDF路径> [起始页 [结束页]]`。§6.10 规定的
  "双栏页面必须用管线真实输出核对、不能凭肉眼扫读"就是靠这个工具执行，不用每次现场重新写。
- `scripts/text-normalize.py`：后处理（标点规范化、断词、未完句合并、标题识别，含 `field` 类型跳过合并的修复）。
- Apple Vision OCR：`ssh chacesm2max 'swift ~/bin/vocr.swift <图片路径>'`，抽取脚本内部已经在标题乱码场景
  自动调用，独立需要 OCR 时也可以直接这样手动调。

## 5. 通读与验收

跟 lecture-notes §8 同样的要求：**跑通管线 ≠ 内容通顺**，尤其这个场景的文档比期刊论文杂得多，
更不能只信自动化输出。至少做到：
- 标题单独跟原页面截图核对一遍（最容易出乱码，OCR 修复也可能有残留误差）。
- 逐块扫一遍有没有「读不断句的长段落」（信息图页的典型症状）。
- 字段行（`field` 类型块）跟原文件核对内容和数量是否对得上。

## 6. 逐页图文标注：产出可审阅的完整 Markdown

260924 实测固化的产出格式——不只是把文字和图片都抽出来，而是按「一页一页」的顺序拼成一份人能直接
审阅的 Markdown，每页结构固定两段：

```
## PDF 第 N 页（内页页码 M）

### 正常排版正文
...（该页的 p/h/field/lead 等正常文字块）

### 本页图片（共 X 张，按从上到下、同行从左到右排序）
...（该页每张图片的三段式标注，见下）
```

**没有图片的页面，直接不写"### 本页图片"这个标题和任何内容，不要写"### 本页图片\n无"。**
纯文字页占多数（HOSA-1 实测 23 页里 10 页没有图片），每页都硬写一句"无"会让文档充满噪音，
读者扫一眼标题层级就知道有没有图，不需要用一行"无"去确认空集合。

### 6.1 页码标注：两个数字大概率相等，但必须每次核对，不能假设

「PDF 第 N 页」指物理页序（从 1 开始数，封面算第 1 页）；「内页页码」指页眉/页脚印出来的编号。
实测 HOSA-1 这份文档两者对每一页都相等（封面不印页码，第 2 页开始印"2"，第 N 页印"N"），
但**这是这份文档恰好如此，不是通用规律**，换一份材料完全可能出现前置页（目录、致谢页等）不计入
页码、导致两个数字对不上的情况。处理前必须先渲染几页真实看一眼页眉，不能直接假设「内页页码=物理页码-1」
或「两者相等」。260925 用 AS0055 实测又遇到一种新组合：**内页页码 = PDF 物理页码 − 1**（封面和
末页两端都不印页码，中间页从 1 印到 10），跟 HOSA 系列的约定不一样，进一步印证"每份文档都要单独
核对，不能照搬上一份材料的经验"。

**印刷页码不一定在页眉/页脚，也可能印在页面角落**：AS0055 这份文档页眉/页脚区域是空的，真正的
页码印在页面**左上角**（`get_text("dict")` 里 bbox 大致 `y∈[36,46]`、`x∈[21,35]` 的一个独立小
数字文本块），一开始只查常规页眉/页脚区域会误以为"这份文档没印页码"。核对页码前，除了看页面
顶部/底部的居中或居左区域，也要扫一遍四个角落有没有单独的小号数字文本块，可以用这样一段代码
快速定位：

```python
for b in pg.get_text("dict")["blocks"]:
    if b["type"] != 0: continue
    for l in b["lines"]:
        txt = "".join(s["text"] for s in l["spans"]).strip()
        if txt.isdigit() and len(txt) <= 4:
            print(l["bbox"], repr(txt))  # 看这个数字block落在页面哪个位置
```

**这一步最容易手滑出错**：260924 实测里，手动拼 Markdown 时把索引页 7 的图片错接到索引页 6 的文字
段落下面（两页内容其实完全不同），也把好几页的"内页页码"数字标错过。**每写完几页要用
`grep -n "^## PDF 第" 文件.md` 扫一遍标题序列，确认页码连续、不重不漏**，比事后通读校对更快发现问题。

### 6.2 图片三段式标注格式

```
**图片 N**（视觉位置说明，如"左上""右侧中部"）
【图片内文字（OCR）】：...
【图片内容描述（视觉识别）】：...
【关联文字】：...
```

- **【图片内文字】**：图片像素里本身带的文字（图表坐标轴/图例/标题、截图里的文档正文），靠
  `ssh chacesm2max 'swift ~/bin/vocr.swift <图片路径>'` 抠，**但 OCR 结果不能直接照搬**：
  实测在小尺寸图表（如问卷统计条形图）上出错率很高（"amplifies emotional response"被识别成
  "amoitn nmotional mancnaey"这种完全不可用的乱码），**不能直接照抄 OCR 结果**，必须由执行这个
  skill 的 AI（多模态模型）自己直接看原图逐字核对修正——这一步不是让用户去做人工校对，是模型
  自己的视觉理解能力去核对 OCR 的输出对不对，在标注里写明"已核对修正 N 处 OCR 识别误差"；OCR
  完全失败（返回空或不可用）时如实写"未能可靠识别"，不要编。
- **【图片内容描述】**：图片里除文字外的视觉元素，**靠自己直接看图写，不是 OCR 能做的事**。
  如果是**图表**（条形图/折线图/散点图等），必须写清楚细节——图表类型、坐标轴代表什么、
  哪个选项/类别数值最高最低、整体趋势走向，不能只写"这是一张图表"；如果原始数据在页面别处
  已经有文字资料（比如这批图表旁边正好有对应的文字分析段落，或者同一份文档另一页有同一批数据的
  试卷原文），要点出这层对应关系，不要当成孤立信息重复抄一遍。如果是照片，正常描述画面内容即可，
  不需要过度解读画面里在做什么活动（能从旁边关联文字确认的才写，不能确认的只写客观画面）。
- **图片是规整数据表格（行列对齐、有表头，不是曲线图/柱状图这类图形化图表）时，
  【图片内文字】直接还原成真正的 Markdown 表格**，不要写成一条条项目符号列表。260925 用
  AS0773 实测：一张"Table 1"截图（2023/2024 实体音乐销量对比表）最初写成了"CD：16.2 → 16.8
  （+3.3%）"这种逐行文字描述，用户明确反馈"这种清晰的表格，你是不是可以还原成 markdown 格式的
  表格"——这类图片本身就是结构化数据，还原成 `| 列1 | 列2 | ... |` 的 Markdown 表格既更省字数、
  也让读者能直接引用/复制数字，比转写成散文段落更贴近原始信息结构。判断标准：图片里的内容是不是
  可以清楚划分出统一的行和列（每行字段数一致、有表头行）——是的话就用表格，图表/统计图（饼图、
  柱状图、折线图）这类没有行列结构的仍然按上面的图表描述方法写。
- **【关联文字】**：页面正文里跟这张图片位置相邻、但不是走正常段落流的文字——图片下方的说明
  文字（caption）、图片左右两侧环绕的窄栏文字。这部分文字来自 PDF 正常文字层（不是 OCR），
  判断方法是几何位置：跟图片 y 范围相邻、x 范围有交集的文字块。多张图片共用同一条说明文字时
  要写清楚"对应全部 N 张图片共用"；判断不出对应哪张时坦诚写"经坐标核对无法确定唯一对应"，
  不要硬指给某一张。

### 6.3 比例原则：不是每张图都要同等篇幅

一份材料里同时出现「有真实数据的图表」（如学生测验结果统计图）和「纯装饰/标题卡类缩略图」
（如 PPT 幻灯片截图里大量的章节过渡页、纯标题页）时，**详略要按信息密度分配**，不能一刀切都
写成完整三段式长文本：
- 真实数据图表（測验统计、问卷结果）：按 §6.2 完整展开，数据要写全。
- 纯标题卡/过渡页缩略图：一两句话说清标题和版式即可，没必要逐条抄排版细节。
- 内容与全文其他位置重复的图表/照片（如同一批"Presenters giving lecture"照片反复出现）：
  可以简要标注"跟前面 XX 页同一场景"，不用每次重新详细描述。

判断某张图是否值得详写，可以先问自己：**这张图里的信息，读者不看图能不能从上下文猜到大概？**
猜不到（比如具体的百分比数字、图表走势）就要写清楚；猜得到（比如又一张"章节过渡卡"）就可以从简。

### 6.4 已知限制：多张图片彼此紧贴、无文字间隙时裁剪会失败

`extract_images()` 的裁剪边界收紧逻辑（`tighten_rect()`）靠图片周围的文字当"围栏"，
当页面上多张照片直接拼在一起、中间没有任何文字分隔时（如相册九宫格式排版），脚本找不到围栏，
裁出来的图片文件会互相重叠/内容混杂，**不要硬着头皮照抠出来的文件写描述**。这种情况下：
先渲染整页原图（不裁切），由执行 skill 的 AI 直接看着整页原图数一数有几张真实照片、每张的
大致内容和位置，基于整页原图直接写描述（是 AI 自己看图写的，不是等人去看），并在文档里如实
标注"这几张图片自动裁剪不完整，以下描述基于整页原图重新核对"，不要假装是自动化裁剪产出的。

**`tighten_rect()` 本身也踩过"收得太狠"的坑**：`get_image_rects()` 声明的图片矩形有时比肉眼
可见的实际范围更大（PPT/Canva 导出常见，顶部越过上方正文最后一行、底部越过页面边界），第一版
`tighten_rect()` 用四条边都往里收的逻辑去修正，结果在图片彼此紧邻的页面上（HOSA-1 第 8 页两张
并排照片）找错了围栏文字行，把照片本体也一并切掉了，比不修还差。改法是**只收上下两边，且只信
"离声明边界足够近"（`<= max_shift=40pt`）的文字行当围栏**，左右两边不动、距离太远的文字行不
采信——宁可留一点轻微的正文渗入（后续 AI 写描述时凭视觉判断能过滤掉这点渗入，不需要靠人去看），
也不能让裁剪框比实际图片内容本身还小。

### 6.4b 整页背景纹理图会被误判成"有图片"，但过滤条件不能只看面积比例

260925 用 HOSA-2 实测发现：Canva/Slides 导出的 PDF 常把全页背景（斑驳纹理、渐变色块）也存成
一个跟页面等大的图片 XObject，纯文字页因此被 `extract_images()` 误判成"这一页有一张图"，实际
那张图跟正文内容毫无关系，写进交付物只会占篇幅、没有信息量。**判据**：图片声明的矩形面积占
页面面积的比例超过 `max_page_ratio`（默认 85%）**且这一页还有其他实质文字内容**（`page_text_len
>= 50`）才当背景丢弃。**这两个条件缺一不可，只看面积比例会连累封面页**：封面页整页就是一张
插画、标题和团队信息是画在图里的美术字（PDF 文字层这页是空的），面积比例同样接近或等于 100%，
但这张图本身就是唯一、有意义的内容，不是背景。加上"该页还有没有其他文字"这个条件后，封面这种
"文字层空、图是唯一内容"的页面会被正确保留，只有"图是背景、页面另有实质正文"的场景才会被过滤。

### 6.5 交付时是否要带图片文件

产出的 Markdown 里图片是相对路径引用（如 `images/xxx.png`），**只传 Markdown 不传 images
文件夹的话，文档里所有图片链接都会失效**，读者看到的只有文字描述、看不到图。视审阅场景决定：
只要看文字内容和数据核对结果，可以只传 md；需要边看文字边核对原图，要把 images 文件夹一起传，
两者路径关系保持不变（md 和 images 文件夹要在同一目录下）。

### 6.6 复合网格图片：一张图片内部藏了一整个网格的子图/子图表

260925 用 HOSA-2 实测发现的新场景，跟 §6.4「多张图片紧贴无间隙」看着像但**本质不同**：§6.4 是
PDF 里真的有多个独立的图片对象、只是彼此挨得太近导致裁剪脚本分不开边界；这里是 **PDF 结构里就
只有一个图片对象**（比如 Google Slides"网格视图"导出的截图、问卷平台把多题统计图拼在一张图里
导出），`get_image_rects()` 天然拿不到内部子格的坐标，`extract_images()` 抠出来的就是一整张
拼图，没法从 PDF 结构层面再往下拆——这种情况不是裁剪逻辑没做好，是这一层压根没有更细的结构信息
可用，必须换一种识别思路。

**用 `scripts/grid_split.py` 检测并切分内部网格**：

```bash
# 优先自动检测（格子间有干净背景间隔时，如 Slides 缩略图网格）
./venv/bin/python scripts/grid_split.py 复合图.png 输出目录

# 自动检测失败时（格子内容紧贴边界，如图表+表格拼接、没有统一背景间隔），
# 改成手动指定行列数强制等分——先自己看一眼原图数清楚有几行几列
./venv/bin/python scripts/grid_split.py 复合图.png 输出目录 行数 列数
```

- **自动检测原理**：找背景色的行/列条带（gutter），把非背景的内容行/列分组成网格，校验各格
  尺寸是否大致一致（允许 25% 误差，排除误判）。适合缩略图库这种格子间有明显空白间隔的场景。
- **自动检测失败就手动等分**：实测 HOSA-2 一页"11 道题的统计图表拼接图"（2 行 6 列，每格里
  图表和表格紧贴边界、没有干净背景间隔）自动检测直接失败，改成手动数出行列数（`2 6`）强制
  等分裁切。**等分出来的边界不会跟真实格子线像素对齐，但足够看清每格内容**，供 AI 逐格写描述
  够用，不追求像素级精确——目的是让 AI 自己能一格一格看清楚去写描述，不是要产出可以单独使用的
  高保真切图，也不是切给人看的。
- **切出来的空白格子如实说明**：网格是矩形规则结构，格子数（行×列）经常比实际内容数多
  （比如 4×5=20 格但只有 18 张缩略图有内容），多出来的格子切出来是纯背景色，直接在描述里写
  "该格无内容"，不要强行编内容。

**分拆后怎么写进 Markdown**：不改变 §6.2 的「一个 PDF 图片对象 = 一条图片记录」的结构（复合图
在页面里仍然只算一张图），只是在这一条记录的【图片内容描述】里逐格展开描述，而不是笼统写
"一张包含多个子图的复合图"。详略按 §6.3 的比例原则：**装饰性缩略图网格**（如 PPT/Slides 缩略图
画廊）逐格给一两句话；**真实数据网格**（如多题统计图表拼接）逐格按图表标准完整展开（题干、
选项、数值、图表趋势），因为这才是读者真正需要的信息。

### 6.7 页码错位、图片错配页：真实踩过的坑和根因

260925 用 HOSA-2 逐页产出时，用户核对发现**页 14 之前的页码普遍错位**（把第 13 页内容标成第 12 页、
第 12 页标成第 11 页）、**部分图片被归到了前一页**。排查后确认是两个独立的根因叠加，不是同一个 bug：

**根因 1：把图片文件名里的 0 起始页码，当成了最终的 1 起始页标签**

`extract_images()` 生成的文件名是 `{stem}_p{pno:02d}_{idx:02d}.png`，这里的 `pno` 是
`enumerate(doc)` 的 0 起始下标。而这套 skill 的页码约定是「PDF 第 N 页 = pno + 1」（因为封面
一般不印页码，从第 2 个物理页开始页眉印"2"，物理页序号 = pno + 1）。处理纯文字页时，我是靠
`doc[pno].get_text()` 配合页眉数字反复核对得到 `pno+1` 这个换算，做得很仔细；但处理带图片的页
时，看到文件名里现成的 `p08`、`p09` 这类数字，**图省事直接把这个数字当成了"PDF 第 8 页""PDF 第
9 页"来写标题，忘了这是 pno、还要 +1**。两条平行的换算逻辑，一条算对了一条没算，这才是"越往前
错得越明显、到某一页突然又对了"这种规律的真正原因——不是随机出错，是两套方法在同一份文档里各管
一段、没有统一。

**教训**：写代码或写文档时只要出现「同一个量，在不同代码路径/不同地方用两种方式计算」，就必然会有
一条路径被漏掉或算错，不是这次疏忽下次就一定不疏忽。**唯一可靠的做法是只留一处换算**：拿到 pno
（不管是从文件名解析出来的，还是循环变量本身）之后立刻 `pdf_page = pno + 1`，后面全部只用
`pdf_page` 这一个变量，不要在写文档的时候手滑用回 `pno` 本身。

**根因 2：AI 自己看页面渲染图时，会把"标题和图片视觉上挨得近"误判成"它们在同一页"**

HOSA-2 里"2. Models"这个小标题所在的物理页，跟它配的模型照片实际上是两个不同的 PDF 页（标题在
一页的末尾，照片在下一页的开头）——原文档本身就是这样分页的，不是抽取出了问题。第一次处理时我是
看渲染出来的整页截图去描述内容，标题和图片在截图上视觉相邻，很自然地当成"同一页"处理，但这只是
截图渲染的连续视觉效果，不代表它们真的在同一个 PDF 物理页里。

**教训与做法**：判断"这段文字和这张图是不是同一页"，**唯一可靠依据是 `page.get_text("dict")`
里每个 block 自己的页归属（`pno`）和 y 坐标，不能靠"看起来挨得近"**。逐页产出前，先对每一页
（尤其是图片附近）跑一遍下面这样的坐标转储，图片和文字混排的顺序、有没有跨页断在中间，一眼就能
看清楚，不用靠 AI 自己凭视觉猜：

```python
import fitz
doc = fitz.open("文件.pdf")
for pno in range(N1, N2):
    print(f"=====INDEX {pno}（PDF 第 {pno+1} 页）=====")
    for b in doc[pno].get_text("dict")["blocks"]:
        y0 = round(b["bbox"][1])
        if b["type"] == 1:
            print(f"  [IMAGE] y={y0} size={round(b['bbox'][2]-b['bbox'][0])}x{round(b['bbox'][3]-b['bbox'][1])}")
        else:
            txt = "".join(s["text"] for l in b["lines"] for s in l["spans"]).strip()
            if txt: print(f"  [TEXT y={y0}] {txt[:70]!r}")
```

**根因 3（附带发现）：跨会话/跨多次重跑时，凭记忆断定"这个文件名是什么内容"会过期**

抽取管线改一次 bug 就要重跑一次，重跑后同一页的图片数量可能变化（比如这次修了背景图误判，
26 张图片一下变成 12 张），**文件名里的序号是重新编的，不是固定绑定某张具体图片**。这次犯过
一次错：凭"上次看过 p09_01 是教室照片"的记忆直接写描述，没有重新拉取当前这次跑出来的
`p09_01.png` 实际打开看一眼，差点把张冠李戴的内容写进最终文档。

**做法**：每次管线重跑之后，写图片描述前必须重新 `Read` 一遍当前这一轮产出的图片文件确认内容，
**不能用"上一轮看到的内容"替代"这一轮实际的文件"**，哪怕文件名看起来一样。

**根因 4（附带发现）：复合网格图裁切边界不准时，靠"格子在网格里的第几行第几列"推断题号是不可靠的**

`grid_split.py` 手动等分切出来的格子边界只是数学上的均分，不代表真实内容的边界。HOSA-2 页 14
那张图实际是 **2 行 × 5 列**，我第一次凭视觉粗看猜成了 2 行 × 6 列，切出来的格子边界跟真实内容
错位，导致某个格子读出来的标题其实是相邻格子"渗入"进来的（把"第 7 题"读成了"第 8 题"内容渗过
来的乱码）。

**做法**：核对复合网格图内容时，**不要只看切好的单个小格子，要按"整行"甚至整张原图放大**
（比如 `img.crop((0,0,w,h//2)).resize((w*2,h))` 把上半/下半整行放大 2 倍），用卡片上自己打印的
编号/标题文字（如"第 N 题"）去确认内容归属，不要用"这个格子在网格里排第几个"去反推题号——网格
里的视觉排列顺序不一定跟内容自带的编号顺序一致（HOSA-2 这张图实测视觉顺序是 1,2,3,4,5,8,9,10,6,7，
不是 1-10 顺排）。

**收尾自查方法**：整份文档写完后，用 `grep -n "^## PDF 第" 文件.md` 列出全部标题，跟"该页图片
文件名前缀 pno+1"和"该页文字开头能不能在 `doc[pno].get_text()` 里精确找到"两项分别核对一遍，
不能只检查标题数字本身是不是连续递增——连续递增只能说明没漏页、没重复页，不能说明每个数字标对了
（这次的错误就是全部连续递增、只是整体偏移了一位，纯看序列连续性完全发现不了）。

### 6.8 并排拼接图：一张图片横向塞了两道题（不是网格，是"2 选 1 列"拼接）

260925 用 HOSA-3 实测发现的场景，跟 §6.6 的复合网格**表面像（都是"一个 PDF 图片对象里塞了多个
逻辑单元"），但产生原因和识别方法不同**：§6.6 是网格视图/缩略图库整体导出（往往 3 格以上，行列
规则）；这里是问卷平台把**相邻两道题的截图左右拼接成一张图**再导出（比如第 13 题和第 14 题的
题干各占左右半边拼成一张图，两题各自的统计饼图也单独拼成另一张图），页面上看起来像"多栏混排的
跨栏截图"，但用 `page.get_images(full=True)` 查证后发现每个视觉行只是**一个宽高比明显偏宽的
单一光栅图片对象**（比如本例中 488×59、486×205 这两张，宽度接近页面正文宽度，高度只有单栏内容
的量级），且 `get_drawings()` 里没有对应的矢量图形，可以排除"矢量图表被漏抽取"的疑虑。

**判断信号**：一张抽取出来的图片，宽度接近整个版心宽度、但高度只有一栏正常内容的高度（不是完整
一页或半页的比例），且视觉上能看出左右两半各自独立完整（各有自己的标题/图例，中间有留白但不是
网格 gutter 那种规则细缝）——这种"宽而扁"的形状本身就是并排拼接图的信号，比单纯看内容更可靠。

**解法**：直接复用 §6.6 的 `grid_split.py`，但按 **1 行 × 2 列**手动等分，不需要先跑自动检测
（自动检测是为网格设计的，对只有 2 个单元格、且中间留白不规则的拼接图未必稳定，手动指定更快更可靠）：

```bash
./venv/bin/python scripts/grid_split.py 拼接图.png 输出目录 1 2
```

HOSA-3 实测（第 13/14 题标题拼接图、第 13/14 题饼图拼接图）均一刀切干净，左右内容无粘连、无
渗入，可以直接分别 OCR/看图描述。

**写进 Markdown 的方式**：跟 §6.6 一样不改变"一个 PDF 图片对象 = 一条图片记录"的结构，只是在
【图片内容描述】里按"左半部分对应第 N 题……；右半部分对应第 N+1 题……"分别展开，各自走标准的
三段式标注（OCR 文字、视觉描述、关联文字）。因为两道题在原始问卷里是各自独立的逻辑单元，描述里
要明确写清楚"这是两道题拼接成一张图，不是一道题的两个部分"，避免读者误以为是同一题的图文。

### 6.9a 图表与正文混排：小图浮动嵌在段落内部，文字环绕排版

260925 用 NEC 论文 AS1578 实测发现的场景：跟之前处理的文档不同，这类论文里的图表不是独立成段
的大图（前后各空一行、单独占一块版面），而是**小尺寸（通常只有四分之一栏宽左右）插图直接浮动
嵌在段落中间，正文文字像 Word/杂志排版的"文字环绕图片"一样紧贴图片左右两侧变窄流动**，一份
6 页文档里 8 张图几乎全是这种嵌入方式。

**好消息：这个场景在抽取技术层面没有额外障碍**——用 `page.get_text("dict")["blocks"]` 检查发现，
即使图片把正文行挤得变窄，PDF 自身的文字流仍然把同一段落识别、合并成**一个完整的文字块**，不会
因为图片穿插而断句、丢字或错序。用本 skill 一直沿用的"先按 (y, x) 位置排序全部 block、正文文字
和图片各自成段落"的方法，直接就能正确重建完整段落，不需要为这个场景写任何新脚本或新的排序逻辑。

**难点全部在写作层面，不在抽取层面**：
- 判断某张图对应哪一段说明文字时，不能简单"就近取图片正上方/正下方"，因为图片是嵌在段落内部，
  跟它对应的解释句可能在图片前面（引出句，如"shown in left figure below"）也可能在图片后面
  （承接句，如"As suggested in the technology curve above"），甚至跨页衔接（图片在这页，解释句
  在上一页末尾或下一页开头）——要通读图片前后至少一整段正文，找到真正指代这张图的那句话，不要
  只看空间最近的文字。
- 同一版面上如果并排放了两张性质不同的图（如 AS1578 页 2 顶部左侧是有具体百分比的实证柱状图、
  右侧是没有刻度的 MRP 理论曲线图），要先分别判断每张图属于"有数据的图表"还是"纯概念曲线图"，
  再分别按 §6.2（数据图表）或 §6.9b（概念曲线图）对应的方法写，不能用同一套详略标准打包处理，
  也不要靠"两张图挨在一起"就假设内容类型相同。
- 按 §6.2 的图片三段式标注格式和 §6.3 比例原则正常处理即可，只是【关联文字】部分要格外留意
  上面提到的"跨段/跨页寻找指代句"这条，其余部分（OCR、视觉描述）跟正常场景没有区别。

### 6.9b 纯概念性曲线图（无坐标刻度、无具体数值，只有形状/位移带信息）

260925 用 NEC 论文 AS0779 实测发现的场景，跟之前处理过的"有具体数值的统计图表"（饼图百分比、
柱状图数值）完全不同类：经济学论文里常见的手绘/简易绘图工具画的概念图——商业周期图、生产可能性
曲线（PPC）、AD-AS 供需模型、供给-需求曲线——这些图**没有任何刻度和数字，全部信息量只在"线的
形状"和"线相对彼此的位置变化（左右平移/上下起伏）"里**，用户反馈"复杂折线图或我不知道怎么描述"
正是指这一类。这些图在 PDF 结构层面全部是普通光栅图片对象（`get_images()` 能正常抓到，抽取本身
没有技术障碍），难点纯粹在**怎么写描述**，不需要新脚本，需要的是恰当的描述方法：

- **有明确坐标轴标签的规范经济学图（PPC、AD-AS、供给需求曲线等）**：先读出图片里的坐标轴标签
  和曲线自身标注（如"LRAS1/LRAS2""Before AI/After AI""D1/D2"），再看图上有没有箭头指示位移
  方向，把"哪条线变成哪条线、往哪个方向移动、对应经济学里的什么概念（曲线右移/左移/外移）"说清楚，
  这类图虽然抽象但有标准的读图套路（识别曲线标签 → 识别位移箭头 → 对应到"supply/demand
  increase/decrease""curve shifts left/right/outward"这套术语），比想象中更容易写准确。
- **没有坐标刻度、纯粹用波浪线形状表达信息的图（如"实际增长率围绕潜在增长趋势线上下波动"的
  商业周期图）**：不要因为没有数字就跳过或写成"一张示意图"这种空泛描述。改成**逐段描述波浪线
  相对趋势线的位置变化**——从左到右依次说清楚"在哪一段曲线低于趋势线、在哪一段曲线高于趋势线、
  中间穿越了几次、整体末端处于趋势线上方还是下方"，这套"相对位置的序列描述"本身就是这类图唯一
  能传达、也最需要传达的信息，跟正文里"从谷底推向扩张阶段"这类文字描述能直接对应上，比强行编造
  不存在的数值更真实、也更有信息量。
- **判断这类图是不是这个场景**：图片里如果只有"坐标轴文字标签+若干条线+可能有位移箭头"，没有
  任何百分比、具体数字、图例数据，基本可以判定是概念图而非数据图表，直接按上面两种方式之一处理，
  不要试图用 OCR 去抠"数值"（因为根本没有数值可抠）。

### 6.9 图片和正文文字空间重叠：裁切出来的图片里会"渲染进"旁边的正文文字残片

260925 用 HOSA-3 内页页码 5 实测发现的场景：PPT/Canva 类自由排版文档里，装饰性插图/照片经常不走
"图文分栏、各自留白"的规矩，而是**直接叠放在正文文字行上面或彼此叠放**（本例是一张痤疮照片和一张
卡通插画左右下角互相压角，两张图又都压在"We divide our goal into 2 categories, which are
physical and psychological."和"...our bodies. During"这两行正文上）。

**根因**：`extract_images()` 是用 `page.get_pixmap(clip=图片bbox, matrix=...)` 对整页做区域截屏
再裁切，不是直接导出 PDF 图片对象的原始像素。这意味着**裁切框范围内、当前恰好画在那个位置的任何
东西都会被一起截进去**——如果这个位置正好和某一行正文文字的 bbox 有交集，那行正文的文字（哪怕只是
半个单词的尾巴）就会被"烤"进裁出来的图片文件里，跟图片自己设计内容混在一起，肉眼看不出这段文字
是"图片自带的"还是"背后正文渗进来的"。本例中卡通插画裁切图左侧就带出了正文单词"During"的尾部
"ng"，这段文字本身已经在"正常排版正文"里完整收录过一次，如果不加区分地写进【图片内文字 OCR】，
会造成同一段文字重复出现且来源标注错误。

**判断信号**：一张图片的 bbox 如果跟 `page.get_text("dict")` 里某个文字 line 的 bbox 有矩形交集，
就说明这张图片大概率被叠放在正文上，裁切图里可能带渗入文字。快速核对：

```python
def overlaps(a, b):
    return not (a[2] <= b[0] or b[2] <= a[0] or a[3] <= b[1] or b[3] <= a[1])
# a = 图片 bbox, b = 某行文字 bbox，两者都是 (x0,y0,x1,y1)，有交集就是潜在渗入
```

**做法**：写图片的【图片内文字（OCR）】之前，先跑一遍上面的重叠检查——如果图片 bbox 和某行正文
bbox 有交集，OCR 出来的文字里但凡能在这一行正文里找到对应片段（哪怕只是几个字符的残片），就判定
为"正文渗入"而不是"图片自带文字"，在描述里显式说明（例如"图片左侧边缘可见正文单词『During』的
尾部『ng』渗入，这是该图片与正文文字行发生版面重叠导致的裁切残留，不属于图片自身设计内容，正文
已在上方完整收录，此处不重复计入"），不要把这段渗入文字当成图片自己的 OCR 内容照单全收。图片和
图片之间彼此压角重叠（不涉及正文）则只需要在视觉描述里如实说明"两张图片在原文档中相互压角叠放"，
不影响各自裁切文件的独立性（因为是分别按各自 xref 的 bbox 裁切，不会互相污染对方的裁切范围）。

### 6.10 真双栏学术论文：脚本层 3 个真实 bug + AI 整理正文必须用管线真实输出核对，不能凭自己通读转储

260925 用 AS0055（第一份真正的双栏学术排版论文，此前 AS0773/AS0779/AS1578 全是单栏）实测，
一次性暴露了 3 个脚本 bug + 1 个 AI 自己的作业方法错误，全部在同一轮排查里发现，记录在这里防止
以后处理双栏论文时重新踩一遍。

**判断当前页面是单栏还是双栏，不需要额外方法**——`page_columns()` 已经会为每一页独立算出
`starts`（该页的栏首 x 坐标列表），`len(starts) > 1` 就是双栏；这一步管线本身没问题，出问题的
是双栏检测之后的两个环节：脚本怎么把图片和文字按栏正确合并、以及 AI 怎么把管线结果整理成交付物。

**脚本 bug 1——排序退化**：文字块在主循环里本来就按"先走完左栏、再走完右栏"正确追加进
`blocks`，但合并图片时的全局排序键 `(页码, y0, x0)` 对双栏不成立，只要页面上出现图片就会把
已经排好的栏序打散、退化成不分栏的纯 y 坐标排序。改法见下方代码：排序键换成
`(页码, 该内容所属栏的栏首 x 坐标, y0)`，图片按自己的 x0 套用跟文字行相同的"就近栏首"规则
重新归位。

**脚本 bug 2——换栏不清空段落缓冲**：即便排序对了，左栏最后一段和右栏第一段仍会被无声拼成
一段——gap/indent 两个判据都是"跟上一行（同一栏的记录）比"，换栏后第一行在
`last_bottom`/`last_line` 里还没有本栏自己的记录，两个判据双双不触发，没有 `flush()`。改法：
`for x in starts:` 每次换栏都先 `flush()`。**但这一步不能对每页的第一栏也无脑执行**——第一版
把 `flush()` 放在循环最外层，等于单栏文档每页开头都被强制断一次，回归测试用 AS0779 抓到 4 处
横跨页面边界的正常段落被拦腰斩断。正确做法是只在**同一页内**换栏（`col_idx > 0`）才强制
flush，每页第一栏（含换页后的第一栏）交回原有的 gap/indent 逻辑判断，不强制断。

**脚本 bug 3——项目符号悬挂缩进被误判成新段落**：项目符号列表常用"符号行缩进浅、续行缩进深"的
悬挂缩进，跟普通段落"只有首行缩进、续行归零"正好相反，原来的"首行缩进=新段落"判据认不出这种
版式，把一条列表项的每一行续行都拆成独立的一段。改法：用 `BULLET_RE` 识别常见符号字符
（•●○◦▪▫♦‣∙·- *）开头的行，只有这种行才触发新条目的 `flush()`；维护一个 `in_hanging_item`
状态位记录"是否正处于某个列表项的续行"，续行阶段跳过缩进判据。**这里还踩了一个"判据写成互斥
if/elif"的坑**：项目符号行自己往往同时也满足"上一段句末带标点"的 `short_ended` 判据，第一版
把两者写在同一条 if/elif 链里，`short_ended` 分支抢先命中，`in_hanging_item=True` 的赋值根本
没执行，等于没修。**教训**：两个独立的判断（"要不要 flush"和"状态该更新成什么"）即使经常同时
成立，也不能因为"反正只要有一个真就该 flush"而写成互斥分支，必须分开算：

```python
is_bullet_start = bool(BULLET_RE.match(t.strip()))
hard_break = short_ended or gap > PARA_GAP
indent_break = l["b"][0] >= x + BODY * 1.2 and not in_hanging_item
if hard_break or is_bullet_start or indent_break:
    flush()
if is_bullet_start:
    in_hanging_item = True
elif hard_break:
    in_hanging_item = False
# 否则（普通续行/悬挂缩进续行）：in_hanging_item 维持原值不变
```

**AI 自己的作业方法错误（比上面三个脚本 bug 更值得记住，全程没有人参与，是执行 skill 的 AI
自己整理正文时的方法错误）**：三个脚本 bug 全部修完后，用户核查发现交付文档里 PDF 第 4、5、7、8
页（全部是双栏页）仍然是错的——因为第一版交付物的正文根本不是拿管线的实际输出拼出来的，是 AI
对着一份按 `(y0, x0)` 排序的诊断用原始文字转储、凭自己的阅读判断"这句该接哪句"手工重写的。
双栏页面里，左栏最后一行和右栏第一行经常在版面上 y 坐标非常接近（比如都在页面顶部附近），这种
不借助工具、单靠模型自己通读转储的方式极容易把"同一 y 高度附近的两栏内容"误读成"顺着读下来的
同一段叙事"，两栏话题越接近，这种错误就越隐蔽、越不容易在读后自查时被发现（比两栏话题明显不
相关时更难察觉——话题不相关的错误读起来会觉得"不对劲"，话题接近的错误读起来反而很顺）。

**做法（以后处理双栏及以上的论文都要照此执行，AI 不能再单凭自己通读一份原始转储去整理正文）**：
一旦确认某页 `len(starts) > 1`，就必须用管线按栏分组、组内按 y 排序后的**实际处理结果**来核对/
重写正文，直接跑 `scripts/verify-columns.py <PDF路径> [起始页 [结束页]]`，按它打印的"栏 1 全文、
栏 2 全文"顺序逐块核对着写，不能脱离它重新组织语言或凭印象补写。第 6 页丢失的一整句话和三条
项目符号要点，根源不是图片处理丢的，是 AI 自己整理时漏看了原始转储里夹在两栏之间、字号相同、
不显眼的几行——管线按 block 遍历不会跳行漏行，模型自己通读转储会。

（`verify-columns.py` 这个工具本身也是这次事故的产物：当时 SKILL.md 曾经写过"`page_columns()`
是 `run()` 里现成的函数，可以直接调用，不用重新发明"，但 `page_columns()` 当时是定义在 `run()`
内部的嵌套闭包，外部脚本根本拿不到它闭包捕获的 `BODY`/`dead`，那句话本身就是不准确的——真跑起来
的时候只能现场重新抄一遍分栏逻辑，抄的过程里还真的踩了坑。260925 把 `compute_body_and_dead()`
和 `page_columns()` 提成模块级函数，`verify-columns.py` 直接 `import` 复用，这样"能直接调用"
才是真的，不会再有第二份可能跑偏的复制品。）

### 6.11 已知未解决：稀疏栏（某一栏该页只有一两行文字）识别不出来，尝试修过一次但引入更严重的问题

用户直接问过"目前能不能准确识别单栏/双栏"，如实自查后的结论：**多数情况能**（`page_columns()`
按页独立统计候选行 x0 分布，`len(starts)>1` 就是双栏，在 AS0055 的正文页上逐页验证过是对的），
但有一个已知、真实存在、目前没有安全修法的漏洞：**某一栏在某一页上只有一两行内容时（典型场景：
这一栏大部分版面被一张图占掉，只剩一行图注/小标题）会检测不出来**，因为分栏候选要求某个 x 坐标
在本页至少出现 3 次（`cnt[x] < 3: continue`），不满 3 行的那一栏不会被登记成独立栏，内容退回去
归到最近的已知栏；如果这几行恰好和另一栏某几行处在同一视觉行上，`merge_rows()` 还会把它们物理
拼接成同一行文字，比 §6.10 那种段落级粘连更严重，是字符级别的粘连。用合成 PDF 复现过：右栏只放
2 行文字，检测不到，被拼进左栏对应行末尾，变成读不断句的乱码。**目前处理过的所有真实论文都还
没遇到过这个场景**（AS0055 的双栏页两栏文字量都足够），是靠构造合成测试主动找出来的隐患，不是
已经在交付物里发生过的事故。

**试过一次修复，但被验证证伪，已回退**：思路是先扫一遍全文档统计候选行 x0 分布得到文档级栏位
`doc_starts`（同一份论文各页版式通常一致，栏位该在全文档范围内统一确认），每页检测完自己的
`starts` 后，再拿 `doc_starts` 里本页没测到的栏位去核对——只要本页有任意内容落在附近就补回这一
栏。合成测试（2 页文档，第 2 页右栏只放 2 行）验证这个思路本身是对的，正确识别出了稀疏栏。但
**拿到 AS0055 真实文档上跑回归测试，发现直接把参考文献页（单栏，但因为引用条目本身很长，行宽
中位数被算到 500pt+）冲垮了**：判断"本页是否有内容落在文档级第二栏附近"用的容差是
`本页局部 colw * 0.35`，页面越宽（单栏长行页面）这个容差就越松，References 页 colw≈505pt 时
容差松到 ±177pt，宽到把这一页上一堆毫不相关、纯属巧合落在这个 x 范围里的词语片段（比如某条引用
里换行断出来的"References""Discussion""Notes"这几个词）也当成了"第二栏的证据"，最终把一条
完整的引用条目从中间拦腰截断、挪位到了文档完全不相关的位置，比修复前的状态更糟。**教训**：
"容差该按什么基准缩放"这件事，不能想当然用局部变量（本页 colw），因为该变量本身在不同版式的
页面上取值差异巨大（双栏正文页 colw≈270pt，单栏长行页 colw≈500pt+），同一个比例系数在不同页面
上代表的实际像素容差可以差出几倍，宽松到失去意义。

**现状**：这个 fix 已经回退，脚本目前保持"能查出双栏但查不出稀疏栏"的已知局限状态，没有半吊子
的错误修复在里面。以后如果真的在某篇论文里撞见这个场景（某一栏因为被大图占掉、候选行不足 3 条
被漏检），正确的处理路径是**当场针对这一篇具体文档让 AI 自己核实该页的真实分栏情况**（渲染页面截图
让 AI 自己确认，或者用 `get_text("dict")` 直接看这一页所有行的 x0/y0 分布），不要指望脚本自动兜底；
如果要重新尝试自动修复，容差基准必须换成不随页面版式剧烈波动的量（比如固定的 pt 数、或者用
`BODY`〔正文字号〕的倍数，而不是本页的 `colw`），并且除了"临近容差"之外还要加一条"至少 2 行
聚集"的门槛（避免单个孤立的巧合片段就触发误判），修完必须至少用一份真实的双栏+稀疏栏文档
（而不是只用合成 PDF）加上全部已验证单栏/双栏文档做完整回归，再交付。

### 6.11b 单栏文档被图片绕排"假栏"骗成双栏，栏优先排序反而把读序搞乱——已修复

260925 用 AS0128 实测发现的场景，跟 §6.11 的稀疏栏刚好是**反方向的同一类问题**：稀疏栏是"真栏
被漏检"，这里是"假栏被误判成真栏"。单栏文档里如果某处有一张图片、旁边文字绕排变窄（§6.9a 那种
场景，比如"Figure G"配图右侧的"V-III-II. Implications"段落），绕排的这几行只要凑够 3 行、x0
又稳定一致，就会被 `page_columns()` 当成一个独立的"第二栏"，即使整份文档其实是单栏。一旦被
误判成双栏，§6.10 的栏优先排序逻辑就会介入——把本该按从上到下顺序排列的内容，硬按"先走完栏 1
（含误判进来的绕排段落），再走完栏 2"重排，读序完全错乱。实测后果：页面中部一对真正并排展示的
图表（Figure E/Figure F）被排到了页面下方一张完全无关的图片（Figure G）后面，三张图的相对顺序
全部错误。

**用户在处理下一批论文前主动提醒过"有两个图表并排展示的，可能要留意下"，这个提醒直接命中了这个
坑**——并排图表本身的位置排序没问题（同一份单栏文档里，位置排序对并排图片天然能处理对，见下面
"两张真正并排的图表怎么处理"），出问题的是同一页上**恰好还存在**一段绕排文字，把整页误判成双栏，
连累了原本正确的并排图表排序。

**判据（已修复，用两份真实文档的数据校准过）**：区分"真栏"和"绕排产生的假栏"，最先试过用纵向
覆盖跨度（span）做判据，但假栏虽然行数少，图注和绕排段落这两段内容之间跳得开，跨度照样能达到
真栏的一半以上，滤不掉，等于没修。改成看**行数比例**：真正的双栏页面左右两栏行数大致相当（同一
页排满两栏正文，行数不会差太多），假栏的行数则远少于真正贯穿全页的主栏。用 AS0055（真双栏）
5 个页面实测校准：两栏行数比最低 52%（40/77）；AS0128（假栏）只有 26%（11/42），中间有将近
2 倍的安全边界。**行数比不到"行数最多的候选栏"一半的，判定为假栏，丢弃、并入最近的真栏。**
修完用 AS0773/AS0779/AS1578（单栏）+ AS0055（真双栏 5 个页面）做了完整回归，逐字节结果不变，
AS0128 页 5 正确识别回单栏。

**两张真正并排的图表本身怎么处理**：跟 §6.8/§6.9a 记录过的场景一样，只要文档本身被正确识别成
单栏（没有被上面这种假栏坑连累），单栏文档的图文合并排序就是简单的 `(页码, y0, x0)`，两张并排
图片天然会按"同一 y 高度、x0 从小到大"排在一起、紧跟在它们各自的图注后面，不需要任何额外处理。
真正需要小心的不是"图表怎么排"，是"这一页会不会被误判成双栏而连累了本来正确的排序"——每次处理
新文档，只要页面上出现图表（尤其是并排展示的），就该顺手用 `verify-columns.py` 扫一遍全文档，
看有没有哪一页被判成了" 2 column(s)"但视觉上其实是单栏（可以渲染那一页原图让 AI 自己交叉核对），不能
假设"反正是单栏论文，双栏相关的坑都不用管"。

### 6.12 三个格式约定：图片占位标记、原始排版画面感描述、跨栏/跨页续接说明

260925 用 AS0055 重写双栏页面时临场想出来的三个写法，用户反馈"处理得非常好"，现在正式固定成
§6 的标准格式要求（此前只存在于那一次的产出物里，没写进这份指令，按规矩必须补——约定只活在
"这次长什么样"里，下次处理新论文时大概率会被忘记重新发明或干脆漏掉）。

**1. 图片在正文流中的插入位置，用占位标记标出来，不要把图片全丢到文末**

`### 正常排版正文` 里，图片实际嵌入的那个位置（不管是段落之间、还是像 §6.9a 那样浮动在段落
内部），要插入一行占位标记，指向 `### 本页图片` 里对应的编号，格式固定为：

```
（此处嵌入 [图片简称/Figure 编号] 图表，见下方"本页图片"第 N 张）
```

作用类似实体书里的"注 1""注 2"——读者/后续处理者顺着正文往下读，遇到这一行就知道"这里原本有一张
图"，可以按编号跳到图片描述区看完整内容，不用图片描述和正文位置完全脱节。**判断该插在哪里**：
按图片在页面上的真实几何位置（y 坐标，双栏页面还要看栏归属）确定，不是随便挑一个看起来顺眼的
段落缝隙。

**2. 原始排版的"画面感"值得如实描述，不要只抽取内容、丢掉版式信息**

遇到原始 PDF 里比较特殊、且直接影响了抽取难度或判断的排版细节（如无编号项目符号被压缩式换行
打散成好几行、悬挂缩进、字段行居中等），在正文或图片描述附近用一句话如实说明这个排版细节本身
长什么样，不要只给读者"处理干净后的结果"、藏起处理过程中真正发生的事。例如：

```
（左栏到此结束。原文这三条要点在 PDF 里是无编号项目符号"●"，前两条被压缩式排版打散成好几行，
首次抽取时被逐行拆散成碎片，也是这次修复的直接触发点。）
```

这类描述的价值在于让读这份交付物的人（包括未来重新核对这份材料的自己）对原文版式有真实的
画面感，而不是一份完全抹平了排版痕迹的纯文本——尤其在版式本身造成过抽取问题时，这句说明也是
"这里我确实核对过原文版式、不是随手输出"的证据。

**3. 句子跨栏/跨页但没有真正的段落边界时，显式点出"这是同一段"**

按 §6.10 的规矩，双栏内容必须严格按"先左栏、再右栏"分段落展示，即使原文里某句话正好横跨栏/页
边界、中间没有真正的段落间断（比如左栏最后一句在右栏或下一页开头接着写完）。这种情况下，在
被拆开的前半句末尾加一行说明，明确这不是内容被截断丢失，而是版式导致的正常拆分，并指出续接点：

```
（这句话直接接到下一页"However, the overall influence of AI on the consumer economy is a
double-edged sword..."，跟 PDF 第 7 页首句是同一段，未被段落边界打断，读到 PDF 第 7 页时
会看到承接。）
```

不加这句说明的话，读者看到一句话在段落中间戛然而止，容易怀疑是不是抽取漏内容了；加上之后，
读者能清楚分辨"这是版式造成的正常切分"还是"真的丢内容了"，也是自查时区分这两种情况的直接依据。
