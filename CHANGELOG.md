# Changelog

版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)：破坏性改动升主版本号，新场景/新能力升次版本号，bug 修复和文案补充升修订号。

## [1.11.2] — 260925

### Changed
- **GitHub SEO**：仓库更名 `pdf-layout-recognizer` → `pdf-to-markdown`（旧地址自动 301 跳转），让仓库名直接命中"PDF to Markdown"这个核心搜索词。
- 仓库描述改成以「PDF 转 Markdown / PDF to Markdown」开头，补充 20 个 GitHub Topics 标签（`pdf-to-markdown`、`pdf2markdown`、`markdown`、`pdf-parser`、`ocr`、`multi-column`、`claude-code` 等），提升 GitHub 站内搜索和 Google/百度收录命中率。
- README 标题和开篇段落显式加入「PDF to Markdown」「PDF 转 Markdown」关键词，原先只是语义上相关、没有把这个精确短语写出来。

## [1.11.1] — 260925

### Changed
- **重写 README，补充「复杂排版是什么」的大白话说明**：新增开篇场景数字化展示（26 个真实场景，按正文抽取/图表标注/多栏识别三层分类）、多栏排版/图文混排/常规图表/复杂图表四类核心概念的通俗解释，配真实场景截图（已剔除姓名/学校等个人信息，仅保留常规文本内容和版式结构）。
- **修正一处会造成误解的表述**：原文档多处写「人工核对/人工校对」描述 OCR 结果核对、图片描述撰写等步骤，实际上这些步骤全程由执行 skill 的 AI（多模态模型）自己完成，不需要真人介入。已在 `README.md` 和 `SKILL.md` 全文排查并改写相关措辞，避免让人误以为这个流程需要人工干预。

## [1.11.0] — 260925

### Fixed
- **单栏文档被「假栏」骗成双栏**：文字环绕图片产生的窄列（见 §6.9a）如果连续行数够多、x 坐标够稳定，会被分栏检测误判成真正的第二栏，导致「栏优先排序」把原本正确的阅读顺序打乱（典型症状：两张真正并排展示的图表被拆开、错误地排到了页面下方无关内容之后）。用两份真实文档（一份真双栏、一份假栏）的数据校准了判据：比较候选栏之间的行数比例，真栏行数比最低约 52%，假栏只有约 26%，中间留了接近 2 倍的安全边界。
- 此前一版用「纵向覆盖跨度」做判据的尝试被证明区分度不够（假栏虽然行数少，但内容分布跨度可能接近真栏），已放弃改用行数比例。

## [1.9.0] – [1.10.0] — 260925

### Fixed
- **双栏文档换栏处段落被无声拼接**：左栏最后一段和右栏第一段在特定条件下会被错误合并成一段，读起来像是两件不相关的事被硬凑在一起。
- **项目符号悬挂缩进被误判成新段落**：项目符号列表「符号行缩进浅、续行缩进深」的悬挂缩进版式，与普通段落「首行缩进、续行归零」正好相反，原判据把每一行续行都错误拆成独立段落。
- **同一个换栏修复自身引入的回归**：第一版修复为了解决双栏换栏粘连问题，把强制分段逻辑放到了每页开头，结果误伤了单栏文档里横跨页面边界的正常段落续写。

### Added
- `scripts/verify-columns.py`：双栏（及以上）文档核对工具，按「栏 1 全文、栏 2 全文……」真实阅读顺序打印每页分栏结果，供人工核对/重写正文，不再需要凭肉眼判断双栏页面的阅读顺序。
- `page_columns()` / `compute_body_and_dead()` 从 `run()` 内部的嵌套闭包重构成模块级函数，外部脚本可以直接复用同一份分栏逻辑，不会出现「诊断脚本和真实抽取逻辑各自维护、悄悄跑偏」的问题。

## [1.8.0] — 260925

### Added
- 三个格式约定固化为标准输出格式：图片在正文中的插入位置用占位标记标出、原始排版的「画面感」细节如实描述、句子跨栏/跨页但没有真正段落边界时显式说明续接点。

## [1.7.0] – [1.7.1] — 260925

### Fixed
- **双栏文档图文合并排序退化成单栏排序**：图片合并进正文时用的排序键对双栏文档不成立，只要页面上出现图片就会把已经排好的「先左栏、再右栏」顺序打散，退化成不分栏的纯 y 坐标排序，导致左右两栏内容交叉错位。

## [1.6.0] — 260925

### Added
- **图表与正文混排场景**：小尺寸图表直接浮动嵌在段落内部、正文文字环绕变窄的排版（常见于自由排版的报告/答卷），确认抽取管线本身能正确处理，重点是标注方法论——准确判断每张图对应哪一段说明文字，可能跨段甚至跨页。

## [1.5.0] — 260925

### Added
- **纯概念性曲线图的描述方法**：经济学/物理学等学科常见的手绘概念图（商业周期图、供需曲线等），没有坐标刻度和具体数值，只靠「线的形状」和「线相对彼此的位置变化」传达信息，需要专门的读图和描述方法，区别于常规的「有数值的统计图表」。

## [1.4.0] — 260924

### Added
- **规整数据表格直接输出为 Markdown 表格**：图片里的内容如果能清楚划分出统一的行和列，直接还原成 `| 列 | 列 |` 的 Markdown 表格，而不是转写成一条条项目符号或散文段落。

## [1.0.0] – [1.3.0] — 260922–260924

首个可用版本，覆盖学生论文/竞赛作品/项目报告这类版式远比标准学术期刊杂乱的 PDF：

### Added
- 分栏候选判据从「中文字符」放宽成「任意字母」（支持纯英文文档）。
- 居中短行（表单字段、封面署名行）单独成块标记，不被段落合并逻辑误伤。
- 行内嵌入图片留下的占位符字符自动清理。
- 装饰字体导致的标题乱码检测 + OCR 重新识别兜底。
- 三种段落边界判据：首行缩进、空行分段、短行收尾分段，按优先级判断，不互斥。
- DRM/企业加密 PDF 检测方法。
- 逐页图文标注的完整 Markdown 产出格式雏形：正文 + 每张图片的三段式标注（OCR 文字 / 视觉描述 / 关联文字）。
- 复合网格图片（一张图片内部藏了整个网格的子图/子图表）的检测与切分工具 `grid_split.py`。
- 页码错位、图片错配页的系统性根因排查方法。
- 并排拼接图（一张图片横向塞了两个逻辑单元）的识别与切分。
- 图片和正文文字空间重叠（裁切渲染会把重叠区域的正文「烤」进图片文件）的判断与处理。
- 整页背景纹理图误判为「有内容图片」的过滤条件校准。
